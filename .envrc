# @cc [owner:ghuntley,label:build] environment-pinned-shell
# Directory activation MUST use devenv so Rust builds and document inspection
# resolve the repository's pinned toolchain rather than arbitrary host binaries.
eval "$(devenv direnvrc)"
use devenv
